import styled from "styled-components";

export const Container = styled.div`
  width: 100%;
  font-size: 15px;
  font-weight: 600;
  color: var(--textColor);
  text-align: center;
  padding: 8px 0;

  span {
    color: var(--linkColor);
    cursor: pointer;
  }
`;
